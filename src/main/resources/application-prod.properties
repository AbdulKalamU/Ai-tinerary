# Production Configuration Profile

# Database Connection (Expected to be provided by environment variables in prod)
spring.datasource.url=jdbc:postgresql://aws-0-ap-northeast-1.pooler.supabase.com:6543/postgres?prepareThreshold=0
spring.datasource.username=postgres.rnbniopvrkpiaenlxwcz
spring.datasource.password=AiTineraryAdmin2026
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=false

# Flyway Migrations
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration

# Actuator
management.endpoints.web.exposure.include=health,metrics
management.endpoint.health.show-details=never

# Logging
logging.level.root=WARN
logging.level.com.aitinerary=INFO
